import type { BreadcrumbItem } from '@/types';

/**
 * Header layout variant (unused by default sidebar shell).
 * Kept for design-system parity with WashNodo.
 */
export function AppHeader(_props: { breadcrumbs?: BreadcrumbItem[] }) {
    return null;
}
